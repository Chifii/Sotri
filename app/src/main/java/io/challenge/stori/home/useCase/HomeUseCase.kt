package io.challenge.stori.home.useCase

import io.challenge.stori.home.repository.HomeRepository

class HomeUseCase(private val homeRepository: HomeRepository) {
	// fun loadBankAccountData() = homeRepository.loadBankAccountData()
}
