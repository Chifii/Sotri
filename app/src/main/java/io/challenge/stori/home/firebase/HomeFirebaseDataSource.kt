package io.challenge.stori.home.firebase

import io.challenge.stori.firebase.FirebaseManager

class HomeFirebaseDataSource(private val firebaseManager: FirebaseManager) {
	// Implementa la lógica para obtener datos de Firebase y convertirlos en BankAccountData
}
